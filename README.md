# Vibe Coding Workshop

Slides for the "바이브 코딩으로 연구실 홈페이지 만들기" workshop.

Open `index.html` in a browser, or serve the folder with:

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.
